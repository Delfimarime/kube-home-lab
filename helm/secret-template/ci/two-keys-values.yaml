# The ordinary case: a credential is a pair. These are the object store's key names, which name
# the protocol rather than the product — the workload's chart maps them onto whatever the process
# itself calls them, so nothing here has to.
name: object-storage-credentials
keys:
  - ACCESS_KEY
  - SECRET_KEY
