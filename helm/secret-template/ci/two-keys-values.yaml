# The ordinary case: a credential is a pair. These are RustFS's key names, which are environment
# variable names because that chart consumes the Secret through `envFrom`.
name: object-storage-credentials
keys:
  - RUSTFS_ACCESS_KEY
  - RUSTFS_SECRET_KEY
