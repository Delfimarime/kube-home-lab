apiVersion: v2
name: keycloak-instance
description: The environment's Keycloak instance and the one path into it.
type: application

# The chart's own version. There is no appVersion: which Keycloak runs is the operator's to
# decide, from the release its manifests were read at, and stating a second version here would be
# a number nothing reads and everything could disagree with.
#
# Moved for `values.schema.json`: the same values, now stated as a schema Helm enforces on lint,
# template and install rather than as prose plus a handful of `required` calls scattered through
# the templates.
version: 0.2.0
