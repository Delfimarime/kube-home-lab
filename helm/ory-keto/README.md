# ory-keto

![Version: 0.2.0](https://img.shields.io/badge/Version-0.2.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square)

The relationship store's chart, plus the two objects it cannot produce for itself: the network policy its write port's protection depends on, and the ConfigMap its permission model is read from. The upstream values dialect is unchanged.

## Requirements

| Repository | Name | Version |
|------------|------|---------|
| https://k8s.ory.sh/helm/charts | keto | 0.63.0 |

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| keto.deployment.extraVolumeMounts[0].mountPath | string | `"/etc/keto-model"` |  |
| keto.deployment.extraVolumeMounts[0].name | string | `"permission-model"` |  |
| keto.deployment.extraVolumeMounts[0].readOnly | bool | `true` |  |
| keto.deployment.extraVolumes[0].configMap.name | string | `"keto-permission-model"` |  |
| keto.deployment.extraVolumes[0].name | string | `"permission-model"` |  |
| keto.keto.config.namespaces.location | string | `"file:///etc/keto-model/permissions.ts"` |  |
| labels | object | `{}` |  |
| model.configMapName | string | `"keto-permission-model"` |  |
| model.content | string | `"import { Namespace } from \"@ory/permission-namespace-types\"\n\n// A subject the issuer minted. Deliberately the whole model: no objects to act on, so no\n// relation to hold and nothing for a permit to derive.\nclass User implements Namespace {}\n"` |  |
| podSelector."app.kubernetes.io/name" | string | `"keto"` |  |
| ports.read | int | `4466` |  |
| ports.write | int | `4467` |  |
| writeAccessFrom | list | `[]` |  |

----------------------------------------------
Autogenerated from chart metadata using [helm-docs v1.14.2](https://github.com/norwoodj/helm-docs/releases/v1.14.2)
